plugin: community.general.nmap
address: "<<inventory_nmap_address>>"
port: 22, 3128
open: true
strict: false
use_extra_vars: true
compose:
  ansible_user: "{{ proxmox_init_user }}"
  ansible_password: "{{ proxmox_init_user_pwd }}"
